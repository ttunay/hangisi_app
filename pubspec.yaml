name: hangisi_app
description: "Mobil Programlama Final Ödevi"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0 

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^6.1.0

  # Firebase paketleri
  firebase_core: ^3.10.1
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/